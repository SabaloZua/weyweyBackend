{
    "version":2,
    "buildCommand":"node ace migration:run --force",
    "builds":[
        {
        "src":"api/index.ts",
        "use":"@vercel/node"
        }
    ],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"api/index.ts"
        }
    ]
}