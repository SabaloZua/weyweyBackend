{
    "version":2,
    "buildCommand":"node ace migration:run && node ace build",
    "builds":[
        {
        "src":"api/index.js",
        "use":"@vercel/node"
        }
    ],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"api/index.js"
        }
    ]
}